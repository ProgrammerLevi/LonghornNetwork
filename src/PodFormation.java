import java.util.*;

/**
 * Base class for created pods from a graph
 */
public class PodFormation {
    /**
     * Creates a class that helps with organizing
     * students into groups based on connections
     * @param graph, the graph to form pods
     */
    public PodFormation(StudentGraph graph) {
        // Constructor
    }

    /**
     * Form pods based on the connections for each student
     * @param podSize, the size each pod should be at max
     */
    public void formPods(int podSize) {
        // Method signature only
    }
}
